{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Привет!'",
    "check": "prettier --check ./src/**/*.html ./src/**/*.css ./src/**/*.js",
    "format": "prettier --write ./src/**/*.html ./src/**/*.css ./src/**/*.js",
    "lint": "stylelint ./src/**/*.css",
    "lintfix": "stylelint ./src/**/*.css --fix",
    "checkAll": "npm run check && npm run lint",
    "fixAll": "npm run format && npm run lintfix",
    "build": "gulp build",
    "dev": "gulp",
    "fixAndBuild": "npm run fixAll && npm run build",
    "lintfixAndPush": "npm run fixAll && git add . && git commit -m \"another version\" && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "browser-sync": "^2.29.1",
    "cross-env": "^7.0.3",
    "del": "^6.0.0",
    "gulp": "^4.0.2",
    "gulp-concat-css": "^3.1.0",
    "gulp-plumber": "^1.2.1",
    "gulp-postcss": "^10.0.0",
    "html-minifier": "^4.0.0",
    "postcss": "^8.5.1",
    "prettier": "^2.8.7",
    "stylelint": "^15.4.0"
  }
}
